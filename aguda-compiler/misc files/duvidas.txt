
    Onde é que uma sequência de expressões exp;exp;... deverá ser permitida sem o uso necessário de parenteses?
    Sugestão: apenas após o ASSIGN de cada Top-Level declaration
    Perguntar se pode dar as AST dos testes que falham

    "The type of variable declaration let id : type = exp is Unit if exp is of type type. Furthermore, if the declaration appears at the left of a semicolon let id : type = exp1 ; exp2, then the type of id is used to validate exp2"
    - Qual a necessidade de ter um caso específico para quando "let id : type = exp1 ; exp2" ? O type de sequential composition já não trata dessa situação?

    Em que situações é que uma expressão pode não ter um tipo?
